# Trace wall backend (DEFERRED — optional). Until these are set, the wall runs in
# on-device mode (marks persist in the visitor's browser only). To make it
# collective & permanent: apply supabase/migrations/0001_traces.sql, then set:
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
